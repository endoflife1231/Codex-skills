# DataForSEO setup for Codex

The `$seo-dataforseo` skill and its field-filter configuration are already included. Configure the optional `dataforseo` MCP server using [the central Codex integration guide](../../../CODEX_INTEGRATIONS.md).

Before a paid request, run the bundled cost preflight, show the estimate, and request approval when the configured threshold requires it. Never invent live metrics when the MCP server is unavailable.
