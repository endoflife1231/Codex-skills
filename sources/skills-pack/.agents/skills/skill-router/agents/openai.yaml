interface:
  display_name: Skill Router
  short_description: Select the best local skill for a task and locate omitted skills in this large pack. Use when
    no exact skill was named or the right workflow is…
  default_prompt: Use $skill-router to handle this task. Follow the skill workflow, inspect relevant project files,
    and verify the result.
