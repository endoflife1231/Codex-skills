interface:
  display_name: Define Goal
  short_description: Help the user define a concrete, measurable goal before starting work, especially when they
    ask to use the goal tool, create a goal, set an…
  default_prompt: Use $define-goal to turn this intention into a concrete, measurable goal before starting work.
