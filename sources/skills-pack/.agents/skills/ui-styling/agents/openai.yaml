interface:
  display_name: UI Styling
  short_description: Build accessible interfaces with shadcn/ui, Radix UI, Tailwind CSS, responsive layouts, themes,
    and reusable styling patterns.
  default_prompt: Use $ui-styling to handle this task. Follow the skill workflow, inspect relevant project files,
    and verify the result.
