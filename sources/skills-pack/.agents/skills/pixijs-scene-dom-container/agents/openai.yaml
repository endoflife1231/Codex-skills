interface:
  display_name: PixiJS Scene Dom Container
  short_description: Use this skill when overlaying HTML elements on the PixiJS v8 canvas. Covers DOMContainer with
    element, anchor, and scene-graph-driven CSS…
  default_prompt: Use $pixijs-scene-dom-container to handle this task. Follow the skill workflow, inspect relevant
    project files, and verify the result.
