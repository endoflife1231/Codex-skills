interface:
  display_name: Vercel React Native Skills
  short_description: React Native and Expo performance and architecture guidance. Use for mobile UI, lists, animations,
    navigation, images, state, or native dependencies.
  default_prompt: Use $vercel-react-native-skills to handle this task. Follow the skill workflow, inspect relevant
    project files, and verify the result.
