# Hooks

- Use hooks for cross-cutting lifecycle concerns, not hidden business logic.
- Authenticate early, authorize as close as possible to the resource operation.
- Avoid expensive work in global hooks.
- Understand whether a hook applies to the current encapsulation scope.
