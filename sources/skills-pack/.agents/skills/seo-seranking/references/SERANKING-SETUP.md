# SE Ranking setup for Codex

The `$seo-seranking` skill is already installed. Export `SERANKING_API_KEY` in the environment that launches Codex, as described in [the central Codex integration guide](../../../CODEX_INTEGRATIONS.md).

Confirm the variable exists without displaying it. Estimate units before broad or repeated AI-visibility queries and request approval when spend is possible.
