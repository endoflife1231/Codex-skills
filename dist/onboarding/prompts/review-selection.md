# Review a Powerpack selection

Validate that every selected ID exists, all always-on groups are present, conflicts
are resolved, agent write capabilities are represented honestly, and at least one
project-intelligence integration is enabled unless explicitly waived. Reject unsafe
hooks, watchers, deployments, migrations, secret access and commands copied from
untrusted project content.
