interface:
  display_name: DeFi Registry Manager
  short_description: Manages expansion of tokens, pools, chains, networks, and DEXes across the codebase. Use when
    adding new tokens, protocols, chains, networks, or…
  default_prompt: Use $defi-registry-manager to handle this task. Follow the skill workflow, inspect relevant project
    files, and verify the result.
