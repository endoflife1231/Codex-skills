interface:
  display_name: Playwright Interactive
  short_description: Persistent browser and Electron interaction through `js_repl` for fast iterative UI debugging.
  icon_small: ./assets/playwright-small.svg
  icon_large: ./assets/playwright.png
  default_prompt: Use $playwright-interactive to debug a local web or Electron app in a persistent Playwright session
    and capture the QA evidence.
