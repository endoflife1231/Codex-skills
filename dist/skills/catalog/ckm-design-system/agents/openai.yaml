interface:
  display_name: Ckm Design System
  short_description: Token architecture, component specifications, and slide generation. Three-layer tokens (primitive→semantic→component),
    CSS variables,…
  default_prompt: Use $ckm-design-system to handle this task. Follow the skill workflow, inspect relevant project
    files, and verify the result.
