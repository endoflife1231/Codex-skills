interface:
  display_name: PixiJS Scene Text
  short_description: Use this skill when rendering text in PixiJS v8. Covers Text for canvas-quality styled labels,
    BitmapText for cheap per-frame updates via glyph…
  default_prompt: Use $pixijs-scene-text to handle this task. Follow the skill workflow, inspect relevant project
    files, and verify the result.
