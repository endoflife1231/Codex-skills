interface:
  display_name: PixiJS Scene Container
  short_description: Use this skill when grouping, positioning, or transforming display objects in PixiJS v8. Covers
    Container constructor options (isRenderGroup,…
  default_prompt: Use $pixijs-scene-container to handle this task. Follow the skill workflow, inspect relevant project
    files, and verify the result.
