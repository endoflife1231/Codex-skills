interface:
  display_name: SEO Competitor Pages
  short_description: Generate SEO-optimized competitor comparison and alternatives pages. Covers "X vs Y" layouts,
    "alternatives to X" pages, feature matrices, schema…
  default_prompt: Use $seo-competitor-pages for this task. Follow the skill workflow, inspect relevant files and
    prerequisites, and verify the result.
policy:
  allow_implicit_invocation: true
