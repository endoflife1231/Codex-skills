interface:
  display_name: Figma Create Design System Rules
  short_description: Generates custom design system rules for the user's codebase. Use when user says "create design
    system rules", "generate rules for my project", "set…
  icon_small: ./assets/figma-small.svg
  icon_large: ./assets/figma.png
  default_prompt: Use $figma-create-design-system-rules to generate project-specific Figma design system rules and
    prepare an AGENTS.md section.
dependencies:
  tools:
  - type: mcp
    value: figma
    description: Figma MCP server
    transport: streamable_http
    url: https://mcp.figma.com/mcp
