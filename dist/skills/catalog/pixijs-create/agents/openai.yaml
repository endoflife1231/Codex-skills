interface:
  display_name: PixiJS Create
  short_description: Use this skill when scaffolding a new PixiJS v8 project with the create-pixi CLI or adding
    PixiJS to an existing project.
  default_prompt: Use $pixijs-create to handle this task. Follow the skill workflow, inspect relevant project files,
    and verify the result.
