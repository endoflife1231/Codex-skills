interface:
  display_name: PixiJS Events
  short_description: Use this skill when handling pointer, mouse, touch, or wheel input in PixiJS v8. Covers eventMode
    (none, passive, auto, static, dynamic),…
  default_prompt: Use $pixijs-events to handle this task. Follow the skill workflow, inspect relevant project files,
    and verify the result.
