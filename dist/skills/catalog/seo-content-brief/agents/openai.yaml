interface:
  display_name: SEO Content Brief
  short_description: Generate competitive SEO content briefs with per-section word counts, competitor scoring, keyword
    density guidance, and page-type templates.…
  default_prompt: Use $seo-content-brief for this task. Follow the skill workflow, inspect relevant files and prerequisites,
    and verify the result.
policy:
  allow_implicit_invocation: true
