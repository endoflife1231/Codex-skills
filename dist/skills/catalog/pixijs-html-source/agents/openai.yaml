interface:
  display_name: PixiJS Html Source
  short_description: Use this skill when rendering live HTML/DOM elements (or frozen snapshots of them) as PixiJS
    v8 textures via the EXPERIMENTAL HTML-in-Canvas browser…
  default_prompt: Use $pixijs-html-source to handle this task. Follow the skill workflow, inspect relevant project
    files, and verify the result.
