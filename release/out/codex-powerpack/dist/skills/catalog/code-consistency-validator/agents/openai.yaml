interface:
  display_name: Code Consistency Validator
  short_description: Validates type consistency across Rust, TypeScript, PostgreSQL boundaries. Use when reviewing
    code, debugging type mismatches, or validating API…
  default_prompt: Use $code-consistency-validator to handle this task. Follow the skill workflow, inspect relevant
    project files, and verify the result.
