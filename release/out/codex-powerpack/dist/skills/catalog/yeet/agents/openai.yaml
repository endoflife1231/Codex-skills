interface:
  display_name: Yeet
  short_description: Use only when the user explicitly asks to stage, commit, push, and open a GitHub pull request
    in one flow using the GitHub CLI (`gh`).
  icon_small: ./assets/yeet-small.svg
  icon_large: ./assets/yeet.png
  default_prompt: 'Prepare this branch for review: stage intended changes, write a focused commit, and open a PR.'
