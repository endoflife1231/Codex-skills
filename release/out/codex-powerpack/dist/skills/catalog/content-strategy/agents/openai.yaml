interface:
  display_name: Content Strategy
  short_description: When the user wants to plan a content strategy, decide what content to create, or figure out
    what topics to cover. Also use when the user mentions…
  default_prompt: Use $content-strategy for this task. Follow the skill workflow, inspect relevant files and prerequisites,
    and verify the result.
policy:
  allow_implicit_invocation: true
