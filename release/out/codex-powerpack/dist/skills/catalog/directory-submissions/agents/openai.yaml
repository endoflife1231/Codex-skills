interface:
  display_name: Directory Submissions
  short_description: When the user wants to submit their product to startup, SaaS, AI, agent, MCP, no-code, or review
    directories for backlinks, domain rating, and…
  default_prompt: Use $directory-submissions for this task. Follow the skill workflow, inspect relevant files and
    prerequisites, and verify the result.
policy:
  allow_implicit_invocation: false
