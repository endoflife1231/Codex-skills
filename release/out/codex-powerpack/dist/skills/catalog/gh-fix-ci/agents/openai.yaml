interface:
  display_name: GitHub Fix Ci
  short_description: Use when a user asks to debug or fix failing GitHub PR checks that run in GitHub Actions; use
    `gh` to inspect checks and logs, summarize failure…
  icon_small: ./assets/github-small.svg
  icon_large: ./assets/github.png
  default_prompt: Inspect failing GitHub Actions checks in this repo, summarize root cause, and propose a focused
    fix plan.
