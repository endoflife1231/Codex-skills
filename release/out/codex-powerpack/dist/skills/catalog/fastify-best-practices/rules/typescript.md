# TypeScript

- Enable strict mode and avoid unvalidated casts.
- Infer request types from runtime schemas where possible.
- Type decorators with module augmentation and register them before use.
- Separate transport DTOs from domain entities and database row types.
