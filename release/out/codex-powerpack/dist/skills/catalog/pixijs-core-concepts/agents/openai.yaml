interface:
  display_name: PixiJS Core Concepts
  short_description: 'Use this skill when understanding how PixiJS v8 renders frames: the systems-and-pipes renderer,
    the render loop, and how the library adapts to…'
  default_prompt: Use $pixijs-core-concepts to handle this task. Follow the skill workflow, inspect relevant project
    files, and verify the result.
