# Codex Agents

This directory contains the curated Codex-native agent layer for the distribution.

Design goals:

- one canonical agent per role
- clear read/write boundaries
- concise instructions
- no Claude-only tool semantics
- parent-session orchestration

See `registry.json` for the canonical published set.
