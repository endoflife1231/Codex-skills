# Onboarding implementation plan

1. Add schemas, rules and prompt-injection-safe analysis prompts.
2. Build a deterministic scanner with secret/dependency/generated exclusions.
3. Add optional schema-bound `codex exec` analysis and deterministic fallback.
4. Build explainable Skill, agent and integration selection with always-on invariants.
5. Generate a hashed JSON/Markdown plan and compact managed project rules.
6. Extend the existing project installer to consume exact plan selections.
7. Add preflighted apply, adaptation verification and snapshot rollback.
8. Add 18 deterministic fixtures with expected decisions and lifecycle tests.
9. Expand distribution validation/doctor, docs, manifests and release acceptance.

